- hosts: all
  tasks:
      - name: ensure file exists
        copy:
            content: ""
            dest: /tmp/testfile
            force: no
            group: sys
            owner: root
            mode: 0555
